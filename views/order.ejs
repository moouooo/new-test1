<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    订单列表
    <hr>
    <!-- 直接把变量输出，不解析html标签 -->
    <%= content %>
    <hr>
    <!-- 会解析HTML标签 -->
    <%- content %>
    <hr>
    姓名：<%=person.name%>  <br>
    性别：<%=person.sex?"男":"女"%>   <br>
    年龄：<%=person.age%>   <br>
    <!-- 判断条件前面不能加等号 -->
    性别：<% if(person.sex) { %>
        <span style="color:red">男</span> 
        <% }else{ %>
            女
        <%  }   %>
    <br>
    爱好：<% for(var i=0;i<person.fav.length;i++){ %>
        <%= person.fav[i];  %>
    <%} %>
</body>
</html>